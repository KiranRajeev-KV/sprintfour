# Frontend

Frontend-specific run commands:

```bash
bun install
bun run dev
```

Build and test:

```bash
bun run build
bun run test
```

For the main project overview and developer setup, see the repo root [README.md](/home/kr/dev/sprintfour/README.md:1).
